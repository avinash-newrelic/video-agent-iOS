# Scenarios

Scenario manifests for NRSampleApp. One YAML file per scenario. Bundled as resources at build time and read by `ScenarioCatalog` at app launch.

See `../README.md` for the manifest schema and how to add a new scenario.
